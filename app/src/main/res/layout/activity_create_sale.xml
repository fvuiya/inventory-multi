<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".activities.CreateSaleActivity">

    <Spinner
        android:id="@+id/spinner_customer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:background="@drawable/border_radius"
        android:minHeight="48dp"
        app:layout_constraintEnd_toStartOf="@+id/imageButton_add_new_customer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/imageButton_add_new_customer"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:layout_marginEnd="8dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Add New Customer"
        android:padding="8dp"
        android:src="@drawable/ic_add"
        app:layout_constraintBottom_toBottomOf="@id/spinner_customer"
        app:layout_constraintEnd_toStartOf="@id/imageButton_calendar"
        app:layout_constraintStart_toEndOf="@id/spinner_customer"
        app:layout_constraintTop_toTopOf="@id/spinner_customer" />

    <ImageButton
        android:id="@+id/imageButton_calendar"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Select Date"
        android:padding="8dp"
        android:src="@drawable/ic_calendar"
        app:layout_constraintBottom_toBottomOf="@id/spinner_customer"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/imageButton_add_new_customer"
        app:layout_constraintTop_toTopOf="@id/spinner_customer" />

    <TextView
        android:id="@+id/tv_selected_products_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Products:"
        android:textAppearance="@style/TextAppearance.AppCompat.Medium"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/spinner_customer" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_selected_products"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:background="@drawable/border_radius"
        app:layout_constraintBottom_toTopOf="@+id/tv_total_amount"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_selected_products_label" />

    <TextView
        android:id="@+id/tv_total_amount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:text="Total: 0.00"
        android:textAlignment="viewEnd"

        android:padding="10dp"
        android:background="@color/colorPrimary"
        android:textColor="@android:color/white"


        android:textAppearance="@style/TextAppearance.AppCompat.Large"
        app:layout_constraintBottom_toTopOf="@id/button_finalize_sale"
        app:layout_constraintEnd_toEndOf="parent" />

    <Button
        android:id="@+id/button_finalize_sale"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Finalize Sale"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>